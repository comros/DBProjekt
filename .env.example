# ============================================================
# KONFIGURACJA - SKOPIUJ TEN PLIK JAKO .env I UZUPEŁNIJ
# ============================================================

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecommerce_db
DB_USER=postgres
DB_PASSWORD=twoje_haslo_postgres

# MongoDB
MONGO_URI=mongodb://localhost:27017/ecommerce_mongo

# Sesja i JWT (zmień na losowe, długie ciągi znaków!)
SESSION_SECRET=zmien_na_losowy_dlugi_ciag_znakow
JWT_SECRET=zmien_na_inny_losowy_dlugi_ciag_znakow

# Serwer
PORT=3000
NODE_ENV=development
